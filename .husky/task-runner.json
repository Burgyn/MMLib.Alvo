{
   "$schema": "https://alirezanet.github.io/Husky.Net/schema.json",
   "tasks": [
      {
         "name": "design-brief-freshness",
         "group": "pre-commit",
         "command": "scripts/check-brief-freshness",
         "filteringRule": "staged",
         "include": [
            "docs/product/alvo-specifikacia.md",
            "docs/product/baas-analyza.md",
            "docs/design-brief.en.md"
         ]
      },
      {
         "name": "dotnet-format",
         "group": "pre-commit",
         "command": "dotnet",
         "args": [ "format", "--verify-no-changes" ],
         "filteringRule": "staged",
         "include": [ "**/*.cs", "**/*.csproj", "**/*.props", "**/*.targets" ]
      },
      {
         "name": "ring0-tests",
         "group": "pre-commit",
         "command": "scripts/test-ring0",
         "filteringRule": "staged",
         "//": "scripts/** and this file are in the list because they DECIDE what the local gate checks: a commit touching only test-ring0 used to run no hook task at all, so the one file that defines the gate was the one file outside it.",
         "include": [ "**/*.cs", "**/*.csproj", "**/*.props", "**/*.targets", "scripts/**", ".husky/task-runner.json" ]
      }
   ]
}
