# PolicyEngineContractTests is xUnit test code even though it lives in src/, not test/ — it is
# inherited by every IPolicyEngine implementation's own test project, so its [Fact]/[Theory]
# methods follow the same descriptive snake_case convention as test/.editorconfig, for the same
# reason SchemaMigratorContractTests does (see src/MMLib.Alvo.Testing/Migrations/.editorconfig).

[PolicyEngineContractTests.cs]
dotnet_diagnostic.CA1707.severity = none
