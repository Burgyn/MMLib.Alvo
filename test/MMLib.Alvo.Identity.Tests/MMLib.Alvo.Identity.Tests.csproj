<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="../../src/MMLib.Alvo.Identity/MMLib.Alvo.Identity.csproj" />
    <!-- The core, for AddAlvo: every fact here is about how the identity package composes with a real
         Alvo container, never about the package resolved on its own. Sqlite gives those facts a real
         database rather than a fake, which is what the store and the bootstrap seeding need. -->
    <ProjectReference Include="../../src/MMLib.Alvo/MMLib.Alvo.csproj" />
    <ProjectReference Include="../../src/MMLib.Alvo.Data.Sqlite/MMLib.Alvo.Data.Sqlite.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" />
    <!-- The resolver's facts substitute IAlvoUserStore and IRoleCatalogProvider: each is one method,
         and a real store would make "a role the descriptor does not declare" a database-seeding
         exercise rather than a statement about the intersection. -->
    <PackageReference Include="NSubstitute" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
