/* GENERATED by scripts/gen-prototype-fixtures — do not edit.

   Every value below is copied from the repository, byte for byte. Run
   `scripts/gen-prototype-fixtures --check` to prove it has not drifted.

   Source: UnhonouredFeatures.cs, UnhonouredSubsystems.cs, CapabilityReport.cs, ManagementEndpoints.cs, ManagementOperations.cs
*/

export const CAPABILITIES = {
  "honoured": [
    "entities",
    "auth",
    "tenancy",
    "access",
    "formats"
  ],
  "warned": [
    {
      "block": "dynamicEntities",
      "consequence": "no runtime entity can be created and the whole dynamic schema-registry driver is absent, so every governance limit declared here bounds nothing (F7)"
    },
    {
      "block": "automation",
      "consequence": "no rule is ever evaluated, so no declared action runs — which looks exactly like a condition that never matched"
    },
    {
      "block": "templates",
      "consequence": "a template referenced by an after-hook 'email' action is rendered, but one referenced only from an automation rule is not, because no rule is evaluated yet — and a 'bodyFile' is not read on either path"
    },
    {
      "block": "webhooks",
      "consequence": "an endpoint an after-hook posts to is delivered to, but one referenced only from an automation rule never receives anything; and no delivery is signed — 'secretRef' is not read and no Standard Webhooks HMAC header is sent, so a receiver cannot yet verify the sender (7.1), nor is the payload projected per endpoint (#152)"
    },
    {
      "block": "functions",
      "consequence": "no function is ever invoked, on any trigger or schedule it declares"
    }
  ],
  "refused": [
    {
      "slot": "field.validation",
      "consequence": "Field 'validation' is not evaluated yet, so a value the expression forbids is accepted — the field is not constrained at all.",
      "fix": "Remove 'validation'. Enforce the rule in a before-hook once #22 lands, or express it with a facet the API does validate — 'maxLength', 'precision'/'scale', enum 'values' or a 'format'."
    },
    {
      "slot": "field.default",
      "consequence": "Field 'default' is honoured as a literal, but not as a '$cel' expression: a CEL default is evaluated against the caller's context at insert time, which is the 'computed' machinery rather than a column default — so the value would be dropped and the field left null.",
      "fix": "Declare a literal default, which this build emits as a column DEFAULT, or remove 'default' and send the value explicitly on create (#113)."
    },
    {
      "slot": "entity.softDelete",
      "consequence": "Soft delete is not supported yet: a delete would remove the row outright and reads would not exclude it, which is irrecoverable data loss where the schema promises recoverability.",
      "fix": "Remove 'softDelete' or track the soft-delete implementation issue. A flag written as false is not a declaration and maps normally."
    },
    {
      "slot": "rollup.where",
      "consequence": "A rollup's 'where' filter is not evaluated yet: the aggregate is still maintained, but it aggregates every record of the child entity instead of the subset this filter declares — a stored number that is silently wrong rather than absent.",
      "fix": "Remove 'where' and aggregate every child record, or move the distinction into the model: a separate child entity, or a second rollup once filtered rollups land. A partial implementation is deliberately not offered — an aggregate over the wrong row set costs more than this refusal."
    },
    {
      "slot": "trigger.event",
      "consequence": "A wildcard event subscription is not matched yet: no rule fires for it today, and on the build that does implement matching it would subscribe to every entity or every operation of every tenant at once — a cross-tenant fan-out nobody re-reads this descriptor to catch, because the event envelope carries no tenant for a subscription to be scoped by (#153).",
      "fix": "Name the entity and the operation exactly — 'entity.orders.created' rather than 'entity.orders.*' — and declare one rule per pair. Wildcards are refused rather than accepted-and-ignored because a descriptor outlives the build that applied it: accepted today, it becomes the fan-out above on the day matching lands, with nothing between it and delivery."
    },
    {
      "slot": "JSONata",
      "consequence": "JSONata transformations are not evaluated yet: the action still runs, but with Alvo's canonical event envelope as its body instead of the transformation declared here — a delivery that succeeded carrying data you did not declare, which is indistinguishable from a bug in the consumer.",
      "fix": "Use a '{{...}}' template instead (e.g. \"{{new.title}}\"), which this build does render, or remove the transformation and accept the canonical envelope. A partial JSONata implementation is deliberately not offered: silently producing a different payload for the part it does not implement costs more than this refusal. Tracked in #149."
    },
    {
      "slot": "email.data",
      "consequence": "An 'email' action's 'data' is not rendered: it is validated when the descriptor is applied and then read by nothing, so the mail goes out with the referenced template's own subject and body and the values declared here are silently dropped — a message that was delivered without the data you declared, which is indistinguishable from a template bug.",
      "fix": "Move the values into the template's 'subject'/'body' as '{{...}}' placeholders over 'new'/'old'/'event'/'@user.id', which this build does render, or remove 'data'. A 'data.*' placeholder root is new surface and lands with the PR that reads it."
    },
    {
      "slot": "bodyFile",
      "consequence": "A template's 'bodyFile' is not read yet: nothing in this build resolves a path inside a descriptor bundle, so an after-hook rendering this template would send a message with an empty body rather than fail.",
      "fix": "Move the body inline into the template's 'body' — it takes the same '{{...}}' placeholders — or stop referencing this template from an after-hook until bundle files are read."
    },
    {
      "slot": "function",
      "consequence": "The 'function' action is declared in the schema but not implemented in this build, so this hook invokes nothing — no function declared under 'functions' runs, on this hook or on any trigger or schedule it declares elsewhere.",
      "fix": "Use a 'webhook' or 'email' action, which this build does run. Custom functions are an F4 concern and the schema freezes their shape ahead of the implementation."
    },
    {
      "slot": "http.call",
      "consequence": "The 'http.call' action is declared in the schema but not implemented in this build, so this hook makes no request: the URL is never called, and 'headersSecretRef' is never read, so a receiver you believe is being notified is not.",
      "fix": "Declare the target under 'webhooks.endpoints' and use a 'webhook' action instead — that is the managed path, and it is the one this build delivers on."
    },
    {
      "slot": "entity.update",
      "consequence": "The 'entity.update' action is declared in the schema but not implemented in this build, so this hook writes nothing — no record is written or patched on the target entity, and no event is emitted for the write that did not happen.",
      "fix": "Perform the follow-up write through the Data API for now. 'entity.update' lands with automation, where the causation chain it creates can be bounded."
    }
  ],
  "routes": [
    {
      "verb": "GET",
      "path": "/projects/{project}/users",
      "operation": "ManageUsers",
      "level": "admin"
    },
    {
      "verb": "POST",
      "path": "/projects/{project}/users",
      "operation": "ManageUsers",
      "level": "admin"
    },
    {
      "verb": "PUT",
      "path": "/projects/{project}/users/{user:guid}/roles",
      "operation": "ManageUsers",
      "level": "admin"
    },
    {
      "verb": "PUT",
      "path": "/projects/{project}/users/{user:guid}/tenant",
      "operation": "ManageUsers",
      "level": "admin"
    },
    {
      "verb": "PUT",
      "path": "/projects/{project}/users/{user:guid}/disabled",
      "operation": "ManageUsers",
      "level": "admin"
    },
    {
      "verb": "POST",
      "path": "/projects/{project}/users/{user:guid}/credential-reset",
      "operation": "ManageUsers",
      "level": "admin"
    },
    {
      "verb": "GET",
      "path": "/info",
      "operation": "GetInfo",
      "level": "viewer"
    },
    {
      "verb": "GET",
      "path": "/projects",
      "operation": "ListProjects",
      "level": "viewer"
    },
    {
      "verb": "GET",
      "path": "/projects/{project}/descriptor",
      "operation": "GetDescriptor",
      "level": "viewer"
    },
    {
      "verb": "GET",
      "path": "/projects/{project}/revisions",
      "operation": "ListRevisions",
      "level": "viewer"
    },
    {
      "verb": "GET",
      "path": "/projects/{project}/revisions/{revision:int}",
      "operation": "GetRevision",
      "level": "viewer"
    },
    {
      "verb": "GET",
      "path": "/projects/{project}/schema",
      "operation": "GetSchema",
      "level": "viewer"
    },
    {
      "verb": "GET",
      "path": "/projects/{project}/capabilities",
      "operation": "GetCapabilities",
      "level": "viewer"
    },
    {
      "verb": "POST",
      "path": "/projects/{project}/policy/simulate",
      "operation": "SimulatePolicy",
      "level": "viewer"
    },
    {
      "verb": "PUT",
      "path": "/projects/{project}/descriptor",
      "operation": "ApplyDescriptor",
      "level": "developer"
    },
    {
      "verb": "POST",
      "path": "/projects/{project}/revisions/{revision:int}/rollback",
      "operation": "RollbackRevision",
      "level": "developer"
    },
    {
      "verb": "PUT",
      "path": "/ai/connection",
      "operation": "SetAiConnection",
      "level": "admin"
    }
  ]
};
